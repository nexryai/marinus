BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Feed] ADD [isPending] BIT NOT NULL CONSTRAINT [Feed_isPending_df] DEFAULT 1;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
