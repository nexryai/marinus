{
    "outputDirectory": "client/build",
    "builds": [
        {
            "src": "api/index.ts",
            "use": "@vercel/node",
            "config": {
                "includeFiles": [
                    "client/build/**"
                ]
            }
        }
    ]
}